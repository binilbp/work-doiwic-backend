# Do it with clarity 
This is backend for the project do it with clarity.

## Info
- configured with `uv` package manager
- api endpoints are created using `fastapi` framework

## Running
- run the backend using `uv run main.py` command in the backend folder (this file location!)
